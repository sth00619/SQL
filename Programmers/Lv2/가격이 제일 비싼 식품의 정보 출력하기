// FOOD_PRODUCT 테이블에서 가격이 제일 비싼 식품의 식품 ID, 식품 이름, 식품분류, 식품 가격을 조회하는 SQL문을 작성해주세요.

// 상위 n개의 데이터를 출력하기
// Oracle인 경우 WHERE 절에 rownum <=n
// MySQL인 경우 WHERE 절에 LIMIT n

SELECT product_id, product_name, product_cd, category, price 
FROM food_product
ORDER BY price desc
LIMIT 1;
